#include <iostream>
#include <math.h>
using namespace std;

int isPowerOfThree(3, n)
{
    return n / 3;
}

int main()
{
    int n;
    cin >> n;
    if (n == 0)
        return false;
    if (n == 1)
        return true;
    return (n % 3 == 0) && (isPowerOfThree);

    return 0;
}